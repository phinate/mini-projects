# implicit statefulness

Uses function calls to implicitly pass state of a "store" object (in practice just a dict) to functions that need it. Inspired by the Athena framework in the ATLAS experiment.